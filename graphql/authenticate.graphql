mutation {
    authenticate(input: {username: "guest", password: "asd123ASD!"}) {
        jwtToken
    }
}

mutation {
    authenticate(input: {username: "admin", password: "asd123ASD!"}) {
        jwtToken
    }
}